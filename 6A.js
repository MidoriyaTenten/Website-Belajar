document.getElementById('inputForm').addEventListener('submit', function(event) {
    event.preventDefault(); // Prevent the form from submitting

    // Get the input values
    const nama = document.getElementById('nama').value;
    const nis = document.getElementById('nis').value;
    const kelas = document.getElementById('kelas').value;

    // Check the input values
    if (nama.toLowerCase() === 'agha putra perdana'.toLowerCase() && nis === '3797' && kelas.toLowerCase() === '6a') {
        // If the inputs match the criteria, display the No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700001';
        document.getElementById('result').style.display = 'block'; // Show the result
    } else if (nama.toLowerCase() === 'akhiruddin sholeh'.toLowerCase() && nis === '3801' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700002';
        document.getElementById('result').style.display = 'block'; // Show the result
    } else if (nama.toLowerCase() === 'amila yasmin kurniawan'.toLowerCase() && nis === '3804' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700003';
        document.getElementById('result').style.display = 'block'; // Show the result
    } else if (nama.toLowerCase() === 'ana ristanti pertiwi'.toLowerCase() && nis === '4006' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700004';
        document.getElementById('result').style.display = 'block'; // Show the result
    } else if (nama.toLowerCase() === 'asyifa olivia hirnawan'.toLowerCase() && nis === '3809' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700005';
        document.getElementById('result').style.display = 'block'; // Show the result  
    } else if (nama.toLowerCase() === 'brayent firdaus lumempouw'.toLowerCase() && nis === '3812' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700006';
        document.getElementById('result').style.display = 'block'; // Show the result   
    } else if (nama.toLowerCase() === 'deba kusmiaty'.toLowerCase() && nis === '3813' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700007';
        document.getElementById('result').style.display = 'block'; // Show the result
    } else if (nama.toLowerCase() === 'dimas ahmad maulana'.toLowerCase() && nis === '3814' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700008';
        document.getElementById('result').style.display = 'block'; // Show the result
    } else if (nama.toLowerCase() === 'fajar maulana sutara'.toLowerCase() && nis === '3816' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700009';
        document.getElementById('result').style.display = 'block'; // Show the result
    } else if (nama.toLowerCase() === 'fakhirah shakila dzahin'.toLowerCase() && nis === '4084' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700010';
        document.getElementById('result').style.display = 'block'; // Show the result
    } else if (nama.toLowerCase() === 'fakhry ardhani'.toLowerCase() && nis === '3900' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700011';
        document.getElementById('result').style.display = 'block'; // Show the result
    } else if (nama.toLowerCase() === 'fathan richie praditya'.toLowerCase() && nis === '3820' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700012';
        document.getElementById('result').style.display = 'block'; // Show the result
    } else if (nama.toLowerCase() === 'feila sufa qatrinnada'.toLowerCase() && nis === '3901' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700013';
        document.getElementById('result').style.display = 'block'; // Show the result 
    } else if (nama.toLowerCase() === 'hafsha nur afifah'.toLowerCase() && nis === '3822' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700014';
        document.getElementById('result').style.display = 'block'; // Show the result 
    } else if (nama.toLowerCase() === 'hibban kartiano al-khasyi'.toLowerCase() && nis === '3823' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700015';
        document.getElementById('result').style.display = 'block'; // Show the result 
    } else if (nama.toLowerCase() === 'keisya alma permana'.toLowerCase() && nis === '3828' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700016';
        document.getElementById('result').style.display = 'block'; // Show the result 
    } else if (nama.toLowerCase() === 'muhammad azka zulfiansyah'.toLowerCase() && nis === '3831' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700017';
        document.getElementById('result').style.display = 'block'; // Show the result 
    } else if (nama.toLowerCase() === 'muhammad fadhlan gunawan'.toLowerCase() && nis === '3830' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700018';
        document.getElementById('result').style.display = 'block'; // Show the result 
    } else if (nama.toLowerCase() === 'muhammad hasan'.toLowerCase() && nis === '3832' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700019';
        document.getElementById('result').style.display = 'block'; // Show the result 
    } else if (nama.toLowerCase() === 'muhammad ilham al fariz'.toLowerCase() && nis === '3833' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700020';
        document.getElementById('result').style.display = 'block'; // Show the result 
    } else if (nama.toLowerCase() === 'muhammad lukman nulhakim'.toLowerCase() && nis === '3834' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700021';
        document.getElementById('result').style.display = 'block'; // Show the result 
    } else if (nama.toLowerCase() === 'muhammad najib'.toLowerCase() && nis === '3835' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700022';
        document.getElementById('result').style.display = 'block'; // Show the result 
    } else if (nama.toLowerCase() === 'nabil azzam putra ramdani'.toLowerCase() && nis === '3838' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700023';
        document.getElementById('result').style.display = 'block'; // Show the result 
    } else if (nama.toLowerCase() === 'nabilah rahmadistira'.toLowerCase() && nis === '3839' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700024';
        document.getElementById('result').style.display = 'block'; // Show the result 
    } else if (nama.toLowerCase() === 'nasya libya aryani'.toLowerCase() && nis === '3842' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700025';
        document.getElementById('result').style.display = 'block'; // Show the result 
    } else if (nama.toLowerCase() === 'nayyara rasyiqa ali'.toLowerCase() && nis === '3844' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700026';
        document.getElementById('result').style.display = 'block'; // Show the result 
    } else if (nama.toLowerCase() === 'putri miladia assyaziliyah'.toLowerCase() && nis === '3847' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700027';
        document.getElementById('result').style.display = 'block'; // Show the result 
    } else if (nama.toLowerCase() === 'sahab husni abdullah'.toLowerCase() && nis === '3849' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700028';
        document.getElementById('result').style.display = 'block'; // Show the result 
    } else if (nama.toLowerCase() === 'widar pratama'.toLowerCase() && nis === '3854' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700029';
        document.getElementById('result').style.display = 'block'; // Show the result 
    } else if (nama.toLowerCase() === 'xabi axelle noor'.toLowerCase() && nis === '3855' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700030';
        document.getElementById('result').style.display = 'block'; // Show the result 
    } else if (nama.toLowerCase() === 'zahira rekafni firmansyah'.toLowerCase() && nis === '3907' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700031';
        document.getElementById('result').style.display = 'block'; // Show the result 
    } else if (nama.toLowerCase() === 'zahra aprilia'.toLowerCase() && nis === '3857' && kelas.toLowerCase() === '6a') {
        // If the inputs match the new criteria, display the new No Sidanira
        document.getElementById('sidaniraNumber').textContent = '2418700032';
        document.getElementById('result').style.display = 'block'; // Show the result                                                                     
    } else {
        // If the inputs do not match, hide the result
        document.getElementById('result').style.display = 'none';
        alert('Data yang dimasukkan tidak valid. Silakan coba lagi.');
    }
});